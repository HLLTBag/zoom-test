In demo-zoom-in-renderer, zoom sdk can't run in render due to sdk auth callback function never triggered

## Run demo
- `cd demo-zoom-in-renderer` // or `demo-zoom-in-main`
- `npm i`
- `npm run start`